import 'package:flutter/material.dart';

class OrderBookTabWidget extends StatelessWidget {
  const OrderBookTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Order book'),
      ),
    );
  }
}
